<h1>Invoice Index Page</h1>


<% @invoices.each do |invoice| %>
<div id="inv-<%= invoice.id%>">
  <p>Invoice # <%=link_to"#{invoice.id}", admin_invoice_path(invoice.id) %> </p>
  <p>Created: <%= invoice.created_at.strftime("%A, %B %d, %Y") %></p>
</div>
<% end  %>
